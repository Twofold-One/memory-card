type ThemeType = typeof theme;

const theme = {
    colors: {
        primary: '#bbdefb',
        ligth: '#eeffff',
        dark: '#8aacc8',
        text: '#000',
    },
};

export default theme;
export type { ThemeType };
